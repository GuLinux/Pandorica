<messages>
  <message id="auth-mail-sender-name">Gulinux's Videostreaming</message>
  <message id="auth-mail-sender-address">marco@gulinux.net</message>
  <message id="html5_player_template">
    <video id="${player.id}"
	class="video-js vjs-default-skin"
	controls
	${<if:autoplay_option>}${autoplay_option}$</if:autoplay_option>}
	width="640"
	height="264"
	poster="http://video-js.zencoder.com/oceans-clip.jpg"
	preload="auto" data-setup="{}"
	> 
      <source type="${video.mimetype}" src="${video.src}">
      ${<if:video_subs_tracks>}
	<track kind="subtitles" src="/?wtd=bZFgETqWtJv2cGVE&amp;request=resource&amp;resource=owebd2&amp;rand=9" srclang="en" label="English"></track>
      ${</if:video_subs_tracks>}
    </video>
  </message>
<message id="Wt.Auth.template.login">
    <div class="Wt-form Wt-auth-login">
      <h2>${tr:Wt.Auth.login-form-title}</h2>
      <p>${tr:Wt.Auth.login-form-info}</p>
      ${<if:oauth>}
	<div style="text-align: center">
	Login with:
	<div class="btn-group">
	${icons}
	</div>
      </div>
      ${</if:oauth>}
      <div style="padding: 10px; text-align: center;">
	<button type="button" class="btn btn-link" data-toggle="collapse" data-target="#login-form-collapsible">Regular Login</button>
      </div>
      <div id="login-form-collapsible" class="collapse">
      <div class="Wt-fields">
    ${<if:passwords>}
      
          <label for="${id:user-name}">${user-name-label}
            ${user-name-info class="Wt-info"}
      </label>
      ${user-name}

      <label for="${id:password}">${tr:Wt.Auth.password}
            ${password-info class="Wt-info"}
      </label>
      ${password}

      ${<if:remember-me>}

        <label for="${id:remember-me}">${tr:Wt.Auth.remember-me}
          <span class="Wt-info">${remember-me-info}</span>
        </label>
        ${remember-me}

      ${</if:remember-me>}

     ${</if:passwords>}
      </div>
      
      <div class="Wt-buttons">
    ${login}
      </div>
      <div class="Wt-buttons">
    ${lost-password} ${sep} ${register}
      </div>
    </div>
  </div>
  </message>
  
  
  <message id="Wt.Auth.confirmmail.body">
Hello {1},

Thank you for joining my Videostreaming Application!

To complete your registration, please confirm your account by
clicking on the following link or copying the URL into your browser.
 
Please click here to confirm your registration or copy and paste the
following URL into your browser: (Note: be sure to copy the entire
URL, including any part of it which goes onto a second line.)

{3}

Note that the server administrator needs to approve your p before you can start using it.
</message>  

 <message id="Wt.Auth.confirmmail.htmlbody">
    <h3>Hello {1},</h3>
    <p>Thank you for joining my Videostreaming Application!</p>
    <p>To complete your registration, please confirm your
    account by clicking on the following link or copying the URL into
    your browser.</p>
    <p>Please <a href="{3}">click here to confirm</a> your
    registration or copy and paste the following URL into your
    browser: <i>(Note: be sure to copy the entire URL, including any
    part of it which goes onto a second line.)</i></p>
    <b>{3}</b>
    <p>Note that the server administrator needs to approve your p before you can start using it.</p>
  </message>
</messages>